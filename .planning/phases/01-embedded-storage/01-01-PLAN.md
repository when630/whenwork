---
phase: 01-embedded-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main/index.mjs
  - main/lifecycle.mjs
  - main/ipc.mjs
  - main/jobs.mjs
autonomous: true
requirements: [STOR-01]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "분할 전후로 `npm test`가 같은 테스트 수를 통과한다 — 동작이 바뀌지 않았다는 뜻이다 (D-08)"
    - "분할 전후로 `npm run smoke`가 `SMOKE_OK`로 끝난다 (D-08)"
    - "`main/index.mjs`는 부트스트랩 호출만 남고, 앱 수명·IPC·백그라운드 작업이 각각 `main/lifecycle.mjs`·`main/ipc.mjs`·`main/jobs.mjs`에 있다 (D-08)"
    - "세 새 모듈은 서로를 import하지 않고 `lifecycle.mjs`가 만든 `ctx` 객체로만 가변 상태를 공유한다 (순환 참조 없음)"
    - "이 계획은 저장소 코드를 한 줄도 바꾸지 않는다 — `main/db.mjs`와 pg 풀은 그대로다 (D-08의 '저장소 교체는 분할이 끝난 뒤 별도 커밋')"
  artifacts:
    - path: main/lifecycle.mjs
      provides: "ctx 생성, 창·트레이·단축키·앱 수명 이벤트"
      exports: ["bootstrap"]
      min_lines: 250
    - path: main/ipc.mjs
      provides: "모든 ipcMain 핸들러 등록"
      exports: ["registerIpc"]
      min_lines: 250
    - path: main/jobs.mjs
      provides: "백그라운드 작업과 타이머 등록"
      exports: ["scheduleJobs"]
      min_lines: 150
    - path: main/index.mjs
      provides: "엔트리포인트 — bootstrap 호출만"
      contains: "bootstrap"
  key_links:
    - from: "main/index.mjs"
      to: "main/lifecycle.mjs"
      via: "bootstrap() 호출 하나"
      pattern: "bootstrap\\(\\)"
    - from: "main/lifecycle.mjs"
      to: "main/ipc.mjs"
      via: "registerIpc(ctx) — ctx 주입"
      pattern: "registerIpc\\(ctx\\)"
    - from: "main/lifecycle.mjs"
      to: "main/jobs.mjs"
      via: "scheduleJobs(ctx) — ctx 주입"
      pattern: "scheduleJobs\\(ctx\\)"
  prohibitions:
    - "분할을 기회 삼아 동작을 '개선'해서는 안 된다 — 순수 이동이 아닌 변경이 섞이면 뒤따르는 저장소 교체가 무엇 때문에 깨졌는지 가릴 수 없다 (D-08)"
---

<objective>
`main/index.mjs`(1479줄)를 D-08이 정한 대로 `main/lifecycle.mjs` / `main/ipc.mjs` / `main/jobs.mjs`로 3분할한다. **동작을 한 줄도 바꾸지 않는 순수 이동**이며, 저장소 교체(01-02 이후)는 이 분할이 끝난 뒤에 시작한다.

Purpose: 1479줄 한 파일 안에서 저장소를 교체하면 어느 변경이 무엇을 깨뜨렸는지 분리할 수 없다. 기존 테스트와 스모크를 안전망 삼아 먼저 경계를 세워 두면, 뒤따르는 모든 계획이 "이 파일의 이 함수"로 좁혀서 일할 수 있다 (D-08, RESEARCH `## Architecture Patterns > Pattern 1`).

Output: `main/lifecycle.mjs`, `main/ipc.mjs`, `main/jobs.mjs` 신규 3개와 얇아진 `main/index.mjs`.

**범위 밖(이 계획에서 하지 않는다):** 저장소 교체, 레거시 스텁, 큐 재설계, 문구 변경, 죽은 코드 삭제. 전부 뒤 계획의 몫이다.
</objective>

<tracer_note>
이 페이즈의 tracer(계층을 관통하는 첫 end-to-end 슬라이스)는 **01-02의 첫 작업**이다. 이 계획이 그 앞에 오는 이유는 D-08이 "분할을 저장소 교체보다 먼저, 별도 커밋으로" 못박은 잠금 결정이기 때문이다. 잠금 결정이 tracer-first 기본값보다 우선한다.
</tracer_note>

<execution_context>
@D:/AIProject/whenwork/.claude/gsd-core/workflows/execute-plan.md
@D:/AIProject/whenwork/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-embedded-storage/01-CONTEXT.md
@.planning/phases/01-embedded-storage/01-PATTERNS.md
@main/index.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: main/lifecycle.mjs 추출 — ctx 생성과 앱 수명·창·트레이·단축키</name>
  <reversibility rating="reversible">모듈 경계는 같은 프로세스 안의 파일 분할이라 되돌리는 비용이 파일 합치기 한 번이다.</reversibility>
  <files>main/lifecycle.mjs, main/index.mjs</files>
  <read_first>
    - `main/index.mjs` 전체 (특히 41-51행 상수, 366-420행 모듈 상태·queue/settings/db 생성, 789-1039행 창·위치기억·트레이, 1390-1479행 앱 수명)
    - `.planning/phases/01-embedded-storage/01-RESEARCH.md` `## Architecture Patterns > Pattern 1` (ctx 객체 주입 형태)
    - `.planning/phases/01-embedded-storage/01-PATTERNS.md` `### main/lifecycle.mjs / main/ipc.mjs / main/jobs.mjs` (이동 대상 구간과 라인 번호)
    - `main/place.mjs`, `main/settings.mjs`, `main/queue.mjs` (lifecycle이 부르는 팩토리들의 시그니처)
  </read_first>
  <action>
`main/lifecycle.mjs`를 새로 만들고 `export function bootstrap()` 하나를 내보낸다. `bootstrap()`은 (1) `ctx` 객체를 만들고 (2) 창·트레이·단축키·앱 수명 이벤트를 등록한 뒤 (3) `ctx`를 돌려준다.

`ctx`가 들고 가는 가변 상태 — 지금 `index.mjs`가 모듈 스코프 `let`으로 쥐고 있는 것 그대로다: `tray`, `captureWin`, `todayWin`, `todayHiddenAt`, `quitting`, `hotkeyOk`, `dbOnline`, `suppressHide`, `abbrHints`, `lastForeground`, `pendingContext`, `queue`, `settings`, `db`, `SMOKE`.

`ctx`가 들고 가는 함수 참조 — 다른 두 모듈이 불러야 하는 것들: `refreshTrayMenu`, `showCapture`, `showToday`, `toggleToday`, `getCaptureWin`, `getTodayWin`, `placeWindow`, `refreshAbbrHints`, `trackForeground`, `cachedForeground`, `vaultRoot`.

`index.mjs`에서 옮길 구간(순수 이동, 로직 손대지 않음): 41-51행 상수(`HOTKEY`, `PURGE_DAYS`, `TODAY_W/H`, `CAPTURE_H`, `SMOKE`, `ROOT`), 58-364행 `SMOKE_PROBE`/`CAPTURE_PROBE` 문자열, 366-420행 모듈 상태와 `queue`/`settings`/`db` 생성 및 `vaultRoot`, 376-395행 포그라운드 추적, 789-945행 창 관련 전부(`baseWinOpts`·`pinOnTop`·`placeWindow`·`rememberPosition`·`getCaptureWin`·`refreshAbbrHints`·`showCapture`·`getTodayWin`·`toggleToday`·`showToday`), 947-1039행 트레이(`trayImage`·`refreshTrayMenu`), 1390-1479행 `app.setAppUserModelId`·`app.setName`·`app.whenReady`·`before-quit`·`will-quit`·`window-all-closed`.

`SMOKE_PROBE`는 템플릿 리터럴이다 — 안에서 달러-중괄호 보간을 쓰면 렌더러가 아니라 메인에서 보간되어 모듈 초기화가 깨진다(`index.mjs` 54-56행 주석). 옮길 때 문자열 내용을 건드리지 말 것.

`app.whenReady` 안의 타이머 등록 블록(1408-1429행)과 `--smoke` 처리 블록(1432-1468행)은 이번 작업에서 **lifecycle 안에 그대로 둔다**. 타이머 등록은 Task 2에서 `scheduleJobs(ctx)` 한 줄로 바뀌고, IPC 등록은 Task 3에서 `registerIpc(ctx)` 한 줄이 된다.

이 시점의 `main/index.mjs`는 `import { bootstrap } from './lifecycle.mjs'; bootstrap();`과, 아직 옮기지 않은 IPC·백그라운드 코드만 남는다. 아직 `index.mjs`에 남아 있는 코드가 lifecycle의 함수를 필요로 하면 `bootstrap()`이 돌려준 `ctx`에서 꺼내 쓴다.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, 또는 요약 줄의 `# fail`이 0이 아니거나 `# pass` 수가 분할 전보다 적다</fails_when>
    <automated>npm run smoke</automated>
    <fails_when>non-zero exit, 또는 출력에 `SMOKE_OK`가 없다(=`SMOKE_FAIL`이거나 아무 줄도 안 나온다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `main/lifecycle.mjs`가 존재하고 `export function bootstrap(` 문자열을 포함한다
    - `grep -c "ctx" main/lifecycle.mjs`가 10 이상이다 (ctx 객체를 실제로 쓴다)
    - `main/index.mjs`가 `bootstrap()` 호출을 포함한다
    - `npm test`가 exit 0이고, 통과 테스트 수가 분할 직전 실행과 같다
    - `npm run smoke` 출력에 `SMOKE_OK`가 있다
    - 이 작업의 커밋에 저장소 코드가 들어 있지 않다 — `test -z "$(git show --name-only --pretty=format: HEAD -- main/db.mjs)"`
  </acceptance_criteria>
  <done>`main/lifecycle.mjs`가 앱 수명·창·트레이·단축키와 ctx 생성을 맡고, 기존 테스트와 스모크가 분할 전과 동일하게 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: main/jobs.mjs 추출 — 백그라운드 작업과 타이머 등록</name>
  <reversibility rating="reversible">모듈 경계 이동. 되돌리려면 파일을 합치면 된다.</reversibility>
  <files>main/jobs.mjs, main/lifecycle.mjs, main/index.mjs</files>
  <read_first>
    - `main/index.mjs`의 남은 부분 (422-432행 `flush`, 434-458행 `collectAll`, 460-476행 `withAiLog`, 478-604행 주간 리뷰, 665-703행 캘린더, 705-750행 백업, 752-787행 `maybeBrief`, 1174-1210행 완료 제안, 1330-1362행 카드 프리웜)
    - Task 1이 만든 `main/lifecycle.mjs` (ctx 필드 이름과 타이머 등록 블록 위치)
    - `main/brief.mjs`, `main/backup.mjs`, `main/calendar.mjs`, `main/collect.mjs` (jobs가 부르는 함수 시그니처)
  </read_first>
  <action>
`main/jobs.mjs`를 새로 만들고 `export function scheduleJobs(ctx)` 하나를 내보낸다. 지금 `index.mjs`에 흩어져 있는 백그라운드 작업 함수를 전부 이 파일 안으로 옮기고, 각 함수가 모듈 스코프 전역 대신 `ctx`를 인자로 받게 바꾼다(호출부 시그니처 변경 외에 로직은 손대지 않는다).

옮길 함수: `flush`, `collectAll`(+`collecting` 플래그), `withAiLog`, `weekOf`·`makeWeeklyReview`·`maybeReview`·`reviewNotice`(+`reviewing` 플래그), `syncCalendarNow`·`todayEvents`(+`syncingCalendar`·`lastCalendarError`), `backupNow`·`maybeBackup`(+`backingUp`), `maybeBrief`·`dayKey`, `maybeSuggestDone`, `prewarmCards`·`buildResumeCard`·`findProject`·`resumePayload`(+`generatingCards`·`cardFailure`·`lastOpenedProject`·`lastResumeSync`), 그리고 이들이 쓰는 주기 상수(`FLUSH_MS`, `COLLECT_MS`, `COLLECT_DELAY_MS`, `CALENDAR_MS`, `BRIEFING_CHECK_MS`, `RESUME_SYNC_MS`, `CARD_COOLDOWN_MS`, `CARD_STALE_HOURS`, `SETTING_KEYS`는 제외).

IPC 핸들러가 불러야 하는 함수(`collectAll`, `makeWeeklyReview`, `backupNow`, `syncCalendarNow`, `todayEvents`, `buildResumeCard`, `resumePayload`, `flush`, `prewarmCards`)는 `scheduleJobs(ctx)`가 `ctx.jobs = { ... }`에 붙여 둔다 — Task 3의 `ipc.mjs`가 여기서 꺼내 쓴다. `jobs.mjs`는 `ipc.mjs`를 import하지 않는다.

`scheduleJobs(ctx)`는 지금 `app.whenReady` 안에 있는 타이머 등록 블록(주기 flush, 지연 collectAll + 6시간 주기, purge 1회, maybeBrief 지연+주기, maybeBackup 지연+주기, maybeReview 지연+주기, 캘린더 주기)을 **그대로** 옮겨 담는다. `!SMOKE` 조건도 그대로 유지한다. `lifecycle.mjs`의 `app.whenReady` 안에서는 이 블록 자리에 `scheduleJobs(ctx)` 한 줄만 남긴다.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, 또는 요약 줄의 `# fail`이 0이 아니다</fails_when>
    <automated>npm run smoke</automated>
    <fails_when>non-zero exit, 또는 출력에 `SMOKE_OK`가 없다</fails_when>
  </verify>
  <acceptance_criteria>
    - `main/jobs.mjs`가 존재하고 `export function scheduleJobs(ctx)` 문자열을 포함한다
    - `main/lifecycle.mjs`가 `scheduleJobs(ctx)` 호출을 포함한다
    - `grep -c "import" main/jobs.mjs` 결과에 `ipc.mjs` 경로가 없다 (`! grep -q "ipc.mjs" main/jobs.mjs`)
    - `npm test`가 exit 0
    - `npm run smoke` 출력에 `SMOKE_OK`가 있다
  </acceptance_criteria>
  <done>백그라운드 작업 함수와 모든 타이머 등록이 `main/jobs.mjs`로 옮겨졌고, 테스트와 스모크가 그대로 통과한다.</done>
</task>

<task type="auto">
  <name>Task 3: main/ipc.mjs 추출 — 모든 IPC 핸들러 등록과 index.mjs 축소</name>
  <reversibility rating="reversible">모듈 경계 이동. 되돌리려면 파일을 합치면 된다.</reversibility>
  <files>main/ipc.mjs, main/lifecycle.mjs, main/index.mjs</files>
  <read_first>
    - `main/index.mjs`의 남은 IPC 구간 (542-564행 review, 613-663행 settings, 691행 calendar, 728행 backup, 884행 capture:projects, 1042-1172행 capture·today·history·itemOps·issue·nudge·due, 1213-1239행 doneSuggestMute·inbox:classify, 1271-1370행 resume, 1372-1388행 open:url·win:hide·app:open)
    - Task 1이 만든 `main/lifecycle.mjs`, Task 2가 만든 `main/jobs.mjs` (ctx 필드와 `ctx.jobs` 이름)
    - `main/preload.cjs` (렌더러가 부르는 채널 이름 — 하나도 빠뜨리면 안 된다)
    - `.planning/codebase/ARCHITECTURE.md` `IPC Layer` 항목 (채널 목록 대조용)
  </read_first>
  <action>
`main/ipc.mjs`를 새로 만들고 `export function registerIpc(ctx)` 하나를 내보낸다. `index.mjs`에 남아 있는 모든 `ipcMain.handle` / `ipcMain.on` 등록을 이 함수 몸통 안으로 옮긴다. 핸들러가 쓰던 전역(`db`, `queue`, `settings`, `todayWin`, `dbOnline` 등)은 `ctx.db`·`ctx.queue`·… 로, 백그라운드 함수는 `ctx.jobs.*`로, 창·트레이 함수는 `ctx.refreshTrayMenu()`·`ctx.showToday()` 등으로 바꾼다. 핸들러 본문 로직은 손대지 않는다.

`itemOps` 맵과 그 순회 등록 패턴(1104-1128행)은 형태를 유지한 채 `ctx.db`를 쓰도록만 바꾼다.

`lifecycle.mjs`의 `bootstrap()`에서 `ctx`를 만든 직후 `registerIpc(ctx)`를 부른다 — `app.whenReady` 이전이어야 한다(지금도 등록은 모듈 로드 시점이다).

마지막으로 `main/index.mjs`를 엔트리포인트만 남게 줄인다: `lifecycle.mjs`에서 `bootstrap`을 import하고 호출하는 것 외에는 남기지 않는다. 남은 import·상수·함수는 전부 세 모듈 중 하나로 옮겨져 있어야 한다.

세 모듈의 import 방향은 `lifecycle → { ipc, jobs }` 한 방향뿐이다. `ipc.mjs`가 `jobs.mjs`를 import하거나 그 반대가 되면 순환이 생기므로, 서로가 필요한 것은 전부 `ctx`를 통해 주고받는다.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, 또는 요약 줄의 `# fail`이 0이 아니거나 `# pass` 수가 분할 전보다 적다</fails_when>
    <automated>npm run smoke</automated>
    <fails_when>non-zero exit, 또는 출력에 `SMOKE_OK`가 없다</fails_when>
    <automated>node -e "const s=require('node:fs').readFileSync('main/index.mjs','utf8'); const n=s.split('\n').length; if(n>=60){console.error('index.mjs still '+n+' lines');process.exit(1)}"</automated>
    <fails_when>non-zero exit(= stderr에 `index.mjs still N lines`가 찍힌다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `main/ipc.mjs`가 존재하고 `export function registerIpc(ctx)` 문자열을 포함한다
    - `test $(grep -c "ipcMain" main/ipc.mjs) -ge 35` (핸들러가 실제로 전부 옮겨왔다)
    - `main/lifecycle.mjs`가 `registerIpc(ctx)` 호출을 포함한다
    - `! grep -q "jobs.mjs" main/ipc.mjs` 이고 `! grep -q "ipc.mjs" main/jobs.mjs` (순환 참조 없음)
    - `main/index.mjs`가 60줄 미만이다
    - `main/preload.cjs`가 노출하는 모든 채널 이름이 `main/ipc.mjs` 안에 등장한다
    - `npm test`가 exit 0이고 통과 수가 분할 전과 같다
    - `npm run smoke` 출력에 `SMOKE_OK`가 있다
  </acceptance_criteria>
  <done>`main/index.mjs`가 부트스트랩 호출만 남은 얇은 엔트리포인트가 되고, 앱 수명·IPC·백그라운드가 세 모듈로 갈렸으며, 분할 전과 같은 테스트·스모크 결과가 나온다.</done>
</task>

</tasks>

## Artifacts this phase produces

> 이 페이즈 전체가 만드는 심볼. "Owner" 열이 그것을 만드는 계획이다.

| Kind | Symbol / path | Owner |
|---|---|---|
| module | `main/lifecycle.mjs` — `bootstrap()` | 01-01 |
| module | `main/ipc.mjs` — `registerIpc(ctx)`, `saveCapture(ctx, title, context)` | 01-01 / 01-07 |
| module | `main/jobs.mjs` — `scheduleJobs(ctx)`, `replayQueueOnce(ctx)`, `maybeBrief(ctx)`, `purgeOnce(ctx)` | 01-01 / 01-03 |
| module | `main/store.mjs` — `createStore(file)`, `MIGRATIONS`, `schemaTables()`, `NewerSchemaError` | 01-02 |
| function (store) | `open`, `close`, `reopen`, `checkpoint`, `status`, `insertCaptures`, `getViewState`, `logEvent` | 01-02 |
| function (store) | `getProjects`, `createProject`, `updateProject`, `archiveProject`, `moveProject`, `completeItem`, `uncompleteItem`, `assignProject`, `setDue`, `toWaiting`, `renameItem`, `removeItem`, `restoreItem`, `setNote`, `nudgeItem`, `nudgeRestore`, `getInbox`, `purgeDeleted`, `getHistory`, `briefing` | 01-04 |
| function (queue) | `replayPending(consume)` — 기존 `drain()` 대체 | 01-03 |
| ctx field | `ctx.store`, `ctx.storeOk`, `ctx.pending` | 01-02 / 01-03 |
| test | `test/store.test.mjs` | 01-02 / 01-04 |
| test | `test/queue.test.mjs` (재작성) | 01-03 |
| test | `test/capture-crash-smoke.test.mjs` | 01-07 |
| CLI flag | `--inject-capture=<title>`, `--smoke-data=<dir>` (둘 다 `--smoke`와 함께만) | 01-07 |
| npm script | `smoke:crash` | 01-07 |
| stdout marker | `CAPTURE_INJECTED <id>` | 01-07 |
| IPC 응답 필드 | `today:getState`의 `notice`, `pending` | 01-02 / 01-03 |
| file name | `store.sqlite` (`userData/`) | 01-02 |
| file name | `store.corrupt-<타임스탬프>.sqlite` | 01-02 |
| file name | `backups/store-v<이전버전>-<YYYYMMDD>.sqlite` | 01-02 |
| file name | `queue.pending-<epoch>.jsonl` | 01-03 |
| deleted | `main/db.mjs`, `main/backup.mjs`, `test/backup.test.mjs` | 01-06 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC) | 렌더러가 보내는 인자는 신뢰 대상이 아니다. 이 계획은 그 경계를 **옮기기만** 하고 검증 로직을 바꾸지 않는다 |
| main ↔ 로컬 파일(queue.jsonl, settings.json) | 앱만 쓰는 파일. 경로는 `app.getPath('userData')` 고정 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01-01 | Tampering | 순수 이동 중 IPC 인자 검증 로직 누락 | high | mitigate | 핸들러 본문을 그대로 옮기고, `main/preload.cjs`의 모든 채널이 `main/ipc.mjs`에 등장하는지 대조한다. `npm run smoke`가 전 탭을 돌며 회귀를 잡는다 |
| T-01-01-02 | Denial of Service | `SMOKE_PROBE` 템플릿 리터럴을 옮기며 달러-중괄호 보간이 생겨 모듈 초기화가 깨짐 | medium | mitigate | 문자열 내용을 편집하지 않고 통째로 이동. `npm run smoke`가 즉시 드러낸다 |
| T-01-01-03 | Repudiation | 분할 커밋에 동작 변경이 섞여 이후 회귀의 원인을 추적 못 함 | medium | mitigate | 저장소 코드 미변경을 `git diff --stat`으로 확인하고, 저장소 교체는 별도 커밋(01-02)으로 분리 (D-08) |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | 이 페이즈는 신규 패키지를 설치하지 않는다 — RESEARCH.md `## Package Legitimacy Audit`가 신규 설치 0건으로 판정. 설치 작업이 없으므로 legitimacy 체크포인트 대상도 없다 |
</threat_model>

<verification>
- `npm test` exit 0, 통과 수가 분할 전과 동일
- `npm run smoke` 출력에 `SMOKE_OK`
- `main/index.mjs` 60줄 미만
- `main/db.mjs` 미변경
</verification>

<success_criteria>
`main/lifecycle.mjs`·`main/ipc.mjs`·`main/jobs.mjs` 세 파일이 존재하고 서로 import하지 않으며, `main/index.mjs`는 `bootstrap()` 호출만 남고, 분할 전후 `npm test`·`npm run smoke` 결과가 동일하다.
</success_criteria>

<output>
Create `.planning/phases/01-embedded-storage/01-01-SUMMARY.md` when done
</output>
