---
phase: 01-embedded-storage
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - main/store.mjs
  - test/store.test.mjs
autonomous: true
requirements: [STOR-01, STOR-05]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-05가 살아남는다고 정한 함수가 전부 `main/store.mjs`에 있고 임시 파일 DB로 단위 테스트된다"
    - "프로젝트 CRUD·완료/취소·프로젝트 배정·마감·대기 전환·재촉·메모·이름 변경·소프트 삭제/복구/purge·인박스 조회가 새 저장소에서 예전과 같은 의미로 동작한다"
    - "`briefing()`은 제거 대상 테이블·칼럼에 기대지 않고 overdue·due_today·inbox·open_todo·oldest_todo_days·stale_waiting 여섯 값만 계산한다 (RESEARCH Pitfall 1)"
    - "\"N일 전/오늘\" 같은 시간 경계는 전부 JS `Date`로 계산해 문자열 파라미터로 바인딩한다 — SQL 안에서 시간을 계산하지 않는다 (D-10, RESEARCH Pattern 3)"
    - "소프트 삭제와 30일 유예 후 물리 삭제가 유지된다 — 한 키에 사라지는 항목이 있으면 캡처 손실 0건이 성립하지 않는다"
  artifacts:
    - path: main/store.mjs
      provides: "살아남는 CRUD 전부 + 축소된 briefing"
      exports: ["createStore", "schemaTables", "MIGRATIONS", "NewerSchemaError"]
      min_lines: 400
    - path: test/store.test.mjs
      provides: "CRUD·재촉 dedup·소프트 삭제·purge·history·briefing 단위 테스트"
      min_lines: 200
  key_links:
    - from: "main/store.mjs briefing()"
      to: "main/brief.mjs briefingLines()"
      via: "축소된 여섯 필드만 넘겨도 falsy 가드 덕에 브리핑 문구가 깨지지 않는다"
      pattern: "(overdue|due_today|stale_waiting)"
  prohibitions:
    - "예전 SQL을 방언만 바꿔 옮기면서 제거 대상 테이블·칼럼 참조를 남겨서는 안 된다 — 실행 시점에야 터지고, 그때는 아침 브리핑이 조용히 사라지는 형태로 나타난다 (RESEARCH Pitfall 1)"
---

<objective>
D-05가 "살아남는다"고 정한 나머지 함수를 `main/store.mjs`로 옮긴다. 01-02가 캡처·오늘 뷰 한 줄기를 증명했으니, 이번에는 그 경계를 옆으로 넓혀 항목·프로젝트 조작과 완료 이력·아침 브리핑까지 새 저장소가 감당하게 한다.

Purpose: 이 함수들이 다 옮겨져야 01-05가 `main/ipc.mjs`의 위임을 통째로 갈아끼우고 01-06이 `main/db.mjs`를 지울 수 있다.

Output: 완성된 `main/store.mjs`와 그 단위 테스트. **이 계획은 `main/ipc.mjs`를 건드리지 않는다** — 배선 교체는 01-05의 몫이라 같은 wave의 01-03과 파일이 겹치지 않는다.

**이 페이즈가 데이터를 옮기지 않는다는 사실:** 이 계획이 끝나도 작성자의 기존 캡처 이력은 여전히 PostgreSQL에만 있고 새 저장소는 비어 있다. 실제 이전은 Phase 3(DATA-04)의 별도 스크립트 몫이다 — "데이터가 사라졌다"로 오해하지 말 것.
</objective>

<execution_context>
@D:/AIProject/whenwork/.claude/gsd-core/workflows/execute-plan.md
@D:/AIProject/whenwork/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-embedded-storage/01-CONTEXT.md
@.planning/phases/01-embedded-storage/01-RESEARCH.md
@.planning/phases/01-embedded-storage/01-PATTERNS.md
@.planning/phases/01-embedded-storage/01-02-SUMMARY.md
</context>

<research_resolution>
RESEARCH.md가 미해결로 남긴 Open Question 1(A1)은 계획 단계에서 실행 확인했다: `count(*) FILTER (WHERE ...)`는 이 프로젝트가 쓰는 SQLite 두 빌드 모두에서 동작한다(시스템 Node 24.13.0의 SQLite 3.50.4, Electron 43.3.0 번들의 3.53.1). 따라서 `briefing()`은 CASE 문으로 부풀리지 않고 원본과 같은 형태를 유지해도 된다. `NULLS LAST`·`RETURNING`·`AUTOINCREMENT`도 같은 방식으로 확인됐다.
</research_resolution>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 프로젝트·항목 조작 함수 이식</name>
  <reversibility rating="reversible">같은 `store.mjs` 경계 안의 함수 추가다. 의미는 기존 `main/db.mjs`가 원본이라 되돌릴 기준이 남아 있다.</reversibility>
  <files>main/store.mjs, test/store.test.mjs</files>
  <read_first>
    - `main/db.mjs` 251-294행(프로젝트 CRUD·순서 변경), 497-557행(완료/취소·배정·마감·재촉·메모), 618-653행(인박스·대기 전환·이름 변경·소프트 삭제/복구/purge) — SQL과 그 위의 한국어 주석(왜 그렇게 하는가)이 이식 대상이다
    - 01-02가 만든 `main/store.mjs`(팩토리 형태, `withTransaction`, 파라미터 바인딩 관례)와 `test/store.test.mjs`
    - `.planning/phases/01-embedded-storage/01-PATTERNS.md` `### main/store.mjs` (analog 2의 `nudgeItem`·`purgeDeleted` 이식 예시)
    - `renderer/today.js` 1160-1170행, 1618-1625행 (프로젝트 목록이 어떤 필드를 읽는지 — 없는 필드는 옵셔널 체이닝으로 이미 안전하다)
  </read_first>
  <behavior>
    - `createProject('가')` → 정수 id를 돌려주고, 같은 이름으로 다시 부르면 새 행이 생기지 않는다
    - `moveProject(id, 'up')` 뒤 `getProjects()` 순서가 바뀌고 `sort` 값이 0..n으로 정규화된다
    - `assignProject(id, pid)`는 `kind`를 `todo`로 바꾸지만 `assignProject(id, pid, true)`는 `kind`를 그대로 둔다 — 대기 항목이 대기 탭에서 사라지면 안 된다
    - `completeItem` 뒤 `done_at`이 ISO 8601 UTC 문자열이고, `uncompleteItem`이 다시 비운다
    - `nudgeItem`을 30분 안에 두 번 부르면 두 번째는 `repeated: true`이고 `count`가 오르지 않는다. 반환값의 `prev`로 `nudgeRestore`를 부르면 직전 값이 되돌아온다
    - `removeItem` 뒤 `getViewState()`에서 사라지지만 행은 남아 있고, `restoreItem`이 되살린다
    - `purgeDeleted(30)`은 30일보다 오래 전에 지워진 행만 실제로 지우고 그 수를 돌려준다
    - `getInbox()`가 인박스 항목의 `context`를 객체로 돌려준다
    - `getProjects()`는 `status`가 `active`인 것만, `sort`·`id` 순으로 돌려준다
  </behavior>
  <action>
`main/store.mjs`에 다음 함수를 더하고 반환 객체에 넣는다: `getProjects`, `createProject`, `updateProject`, `archiveProject`, `moveProject`, `completeItem`, `uncompleteItem`, `assignProject`, `setDue`, `toWaiting`, `renameItem`, `removeItem`, `restoreItem`, `setNote`, `nudgeItem`, `nudgeRestore`, `getInbox`, `purgeDeleted`.

이식 규칙은 RESEARCH.md의 SQL 변환 표를 그대로 따른다:
- `pool.query(sql, [params])` → `db.prepare(sql).run(...params)` / `.get(...)` / `.all(...)`. 자리표시자는 `$1` 대신 `?`.
- 시각을 넣는 자리는 전부 호출 시점 JS에서 `new Date().toISOString()`을 만들어 파라미터로 넘긴다. "N일 전" 경계도 JS에서 계산해 문자열로 바인딩하고 SQL은 부등호 비교만 한다. PostgreSQL 전용 시간 연산자·캐스트는 한 글자도 옮기지 않는다.
- 충돌 무시 삽입은 `INSERT OR IGNORE`, 값 반환은 `RETURNING`(둘 다 이 SQLite 빌드에서 동작 확인됨).
- `moveProject`처럼 여러 UPDATE가 한 덩어리여야 하는 함수는 01-02가 만든 `withTransaction`으로 감싼다.

의미상 반드시 지킬 것:
- `getProjects()`는 D-11이 뺀 프로젝트 리포 경로 칼럼을 select하지 않는다. 렌더러는 이 필드를 옵셔널 체이닝으로 읽고 있어 없어도 깨지지 않는다.
- `assignProject`의 `keepKind` 분기와 그 위의 주석(대기 항목에 프로젝트만 붙이려던 조작이 대기 해제가 되면 안 된다)을 그대로 가져간다. D-11이 뺀 AI 분류 제안 칼럼을 비우는 부분만 뺀다.
- `nudgeItem`의 30분 dedup 판정은 이미 JS `Date.now()`로 하고 있으므로 로직 그대로, 시각 기록만 문자열 파라미터로 바꾼다. 30분 상수와 그 이유를 적은 주석도 함께 옮긴다.
- 소프트 삭제(`deleted_at` 표시)와 `purgeDeleted`의 유예 기간 개념을 유지한다.
- `getInbox()`는 `context` TEXT를 `JSON.parse`해 객체로 돌려준다(01-02의 `getViewState`와 같은 규칙).

모든 사용자 입력은 파라미터 바인딩으로만 넘긴다.

`test/store.test.mjs`에 `<behavior>`의 아홉 항목을 각각 테스트로 더한다. 임시 파일 DB 관례는 01-02가 세운 것을 그대로 쓴다.
  </action>
  <verify>
    <automated>node --test test/store.test.mjs</automated>
    <fails_when>non-zero exit, 또는 요약 줄의 `# fail`이 0이 아니거나 `# pass`가 24 미만</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, 또는 요약 줄의 `# fail`이 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `main/store.mjs` 반환 객체에 위 18개 함수 이름이 모두 들어 있다
    - `! grep -nE "now\(\)|interval '|::date|current_date|\$[0-9]" main/store.mjs` 가 아무것도 찾지 못한다
    - `! grep -nE "\b(activity|issue|resume_card|repo_state|cal_event|review)\b" main/store.mjs` 가 아무것도 찾지 못한다
    - `node --test test/store.test.mjs` exit 0, 24건 이상 통과
    - `npm test` exit 0
  </acceptance_criteria>
  <done>프로젝트·항목 조작 함수가 전부 새 저장소에서 동작하고, 각각의 의미(대기 유지 배정, 재촉 dedup, 소프트 삭제 유예)가 단위 테스트로 고정된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 완료 이력과 축소된 아침 브리핑 이식</name>
  <reversibility rating="reversible">두 함수 추가. 원본 의미는 `main/db.mjs`와 `main/brief.mjs` 테스트가 기준으로 남아 있다.</reversibility>
  <files>main/store.mjs, test/store.test.mjs</files>
  <read_first>
    - `main/db.mjs` 763-780행(`getHistory`), 783-851행(`briefing` — 무엇을 버리는지 보려면 원본을 읽어야 한다)
    - `main/brief.mjs` 55-95행 (`briefingLines`가 없는 필드를 전부 falsy 가드로 감싸고 있다 — 축소가 안전한 이유)
    - `test/brief.test.mjs` (브리핑 문구 테스트가 어떤 필드를 넣어 부르는지 — 이 테스트는 계속 통과해야 한다)
    - `.planning/phases/01-embedded-storage/01-RESEARCH.md` `## Common Pitfalls > Pitfall 1` (briefing을 방언 번역으로 옮기면 안 되는 이유)
    - Task 1이 확장한 `main/store.mjs`, `test/store.test.mjs`
  </read_first>
  <behavior>
    - `getHistory(7)`가 최근 7일 안에 완료된 항목을 `done_at` 내림차순으로 돌려주고, 8일 전 완료 항목은 빠진다. 반환 형태는 `{ items, commits }`이고 `commits`는 항상 빈 배열이다
    - `briefing()`이 `overdue`·`due_today`·`inbox`·`open_todo`·`oldest_todo_days`·`stale_waiting` 여섯 키를 숫자로 돌려준다
    - 마감이 어제인 항목은 `overdue`에, 오늘인 항목은 `due_today`에 잡히고, 이 판정은 항목의 `kind`와 무관하다 — 인박스·대기 항목의 마감도 센다
    - 5일 넘게 손대지 않은 대기 항목은 `stale_waiting`에 잡히고, 어제 재촉한 대기 항목은 잡히지 않는다 — 경과 시계는 재촉 시점부터 다시 센다
    - 완료됐거나 소프트 삭제된 항목은 어느 숫자에도 잡히지 않는다
    - `briefing()` 결과를 `briefingLines()`에 그대로 넣어도 예외 없이 문자열 배열이 나온다
  </behavior>
  <action>
`main/store.mjs`에 `getHistory(days = 7)`와 `briefing(staleDays = 5)`를 더한다.

`getHistory`는 항목 쿼리만 남긴다. 커밋 목록은 제거 대상 테이블에서 오던 것이라 계산하지 않고 `commits: []`를 고정으로 돌려준다 — 호출부와 렌더러가 이 키를 읽으므로 모양은 유지한다. 기간 경계는 JS에서 계산해 문자열로 바인딩한다.

`briefing`은 **다시 쓴다**. 원본에서 제거 대상 테이블에 기대는 서브쿼리·조인과, D-11이 뺀 완료 제안 칼럼에 기대는 계산은 통째로 뺀다. 남기는 여섯 값과 그 의미:
- `overdue` / `due_today` — 마감이 오늘보다 이르거나 오늘인 미완료·미삭제 항목 수. `kind`로 거르지 않는다(원본 주석의 이유를 그대로 옮긴다: 마감은 어느 탭에 있든 챙겨야 한다).
- `inbox` / `open_todo` — 해당 `kind`의 미완료·미삭제 항목 수.
- `oldest_todo_days` — 가장 오래된 미완료 할 일이 잡힌 지 며칠인가. SQL에서는 해당 그룹의 최소 `captured_at`만 가져오고 날수 계산은 JS에서 한다. 없으면 0.
- `stale_waiting` — 재촉했으면 그 시각, 아니면 잡힌 시각이 `staleDays`일보다 오래된 대기 항목 수.

오늘 날짜(`YYYY-MM-DD`)와 `staleDays`일 전 시각 문자열은 호출 시점 JS에서 만들어 바인딩한다. 조건별 집계는 `count(*) FILTER (WHERE ...)`를 쓴다(위 `<research_resolution>` 참고 — 지원 확인됨).

`main/brief.mjs`는 손대지 않는다. `briefingLines()`가 없는 필드를 전부 falsy 가드로 감싸고 있어 축소된 결과만 넘겨도 문구가 깨지지 않는다.

`test/store.test.mjs`에 `<behavior>`의 여섯 항목을 테스트로 더한다. 마지막 항목은 `main/brief.mjs`의 `briefingLines`를 실제로 import해서 호출하는 형태로 쓴다 — 두 모듈의 계약이 어긋나면 여기서 걸린다.

되돌리기 확인: `stale_waiting` 계산에서 재촉 시각을 무시하도록 바꾸면 해당 테스트가 실패하는 것을 확인하고 원복한다. 결과를 SUMMARY에 적는다.
  </action>
  <verify>
    <automated>node --test test/store.test.mjs</automated>
    <fails_when>non-zero exit, 또는 요약 줄의 `# fail`이 0이 아니거나 `# pass`가 30 미만</fails_when>
    <automated>node --test test/brief.test.mjs</automated>
    <fails_when>non-zero exit, 또는 요약 줄의 `# fail`이 0이 아니다</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, 또는 요약 줄의 `# fail`이 0이 아니다</fails_when>
  </verify>
  <acceptance_criteria>
    - `main/store.mjs` 반환 객체에 `getHistory`와 `briefing`이 있다
    - `briefing()`이 돌려주는 키 집합이 정확히 여섯 개이고 모두 숫자다
    - `test/store.test.mjs`가 `main/brief.mjs`의 `briefingLines`를 import해 축소된 결과로 호출하는 테스트를 포함한다
    - `! grep -nE "\b(activity|issue|resume_card|repo_state|cal_event|review)\b" main/store.mjs` 가 아무것도 찾지 못한다
    - `node --test test/store.test.mjs` exit 0, 30건 이상 통과
    - `npm test` exit 0
    - 되돌리기 확인 기록이 SUMMARY에 있다
  </acceptance_criteria>
  <done>완료 이력 조회와 아침 브리핑 재료가 새 저장소에서 나오고, 축소된 브리핑 결과가 기존 문구 로직을 그대로 통과한다.</done>
</task>

</tasks>

## Artifacts this phase produces

| Kind | Symbol / path | Owner |
|---|---|---|
| module | `main/lifecycle.mjs` — `bootstrap()` | 01-01 |
| module | `main/ipc.mjs` — `registerIpc(ctx)`, `saveCapture(ctx, title, context)` | 01-01 / 01-03 |
| module | `main/jobs.mjs` — `scheduleJobs(ctx)`, `replayQueueOnce(ctx)`, `maybeBrief(ctx)`, `purgeOnce(ctx)` | 01-01 / 01-03 |
| module | `main/store.mjs` — `createStore(file)`, `MIGRATIONS`, `schemaTables()`, `NewerSchemaError` | 01-02 |
| function (store) | `open`, `close`, `reopen`, `checkpoint`, `status`, `insertCaptures`, `getViewState`, `logEvent` | 01-02 |
| function (store) | `getProjects`, `createProject`, `updateProject`, `archiveProject`, `moveProject`, `completeItem`, `uncompleteItem`, `assignProject`, `setDue`, `toWaiting`, `renameItem`, `removeItem`, `restoreItem`, `setNote`, `nudgeItem`, `nudgeRestore`, `getInbox`, `purgeDeleted`, `getHistory`, `briefing` | 01-04 |
| function (queue) | `replayPending(consume)` — 기존 `drain()` 대체 | 01-03 |
| ctx field | `ctx.store`, `ctx.storeOk`, `ctx.pending` | 01-02 / 01-03 |
| test | `test/store.test.mjs` | 01-02 / 01-04 |
| test | `test/queue.test.mjs` (재작성) | 01-03 |
| test | `test/capture-crash-smoke.test.mjs` | 01-07 |
| CLI flag | `--inject-capture=<title>`, `--smoke-data=<dir>` (둘 다 `--smoke`와 함께만) | 01-07 |
| npm script | `smoke:crash` | 01-07 |
| stdout marker | `CAPTURE_INJECTED <id>` | 01-07 |
| IPC 응답 필드 | `today:getState`의 `notice`, `pending` | 01-02 / 01-03 |
| file name | `store.sqlite`, `store.corrupt-<타임스탬프>.sqlite`, `backups/store-v<이전버전>-<YYYYMMDD>.sqlite` | 01-02 |
| file name | `queue.pending-<epoch>.jsonl` | 01-03 |
| deleted | `main/db.mjs`, `main/backup.mjs`, `test/backup.test.mjs` | 01-06 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IPC 인자 → store CRUD | 항목 제목·메모·대기 상대·프로젝트 이름이 SQL에 닿는다 |
| 저장소 파일 | 앱 밖에서 편집·손상될 수 있는 입력 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04-01 | Tampering | `renameItem`·`setNote`·`toWaiting`·`createProject`의 자유 텍스트 인자 | high | mitigate | 전부 `db.prepare(sql).run(...)` 파라미터 바인딩. SQL 문자열 결합 금지 (ASVS V5) |
| T-01-04-02 | Denial of Service | `purgeDeleted`가 경계를 잘못 계산해 되돌릴 수 있는 항목까지 물리 삭제 | high | mitigate | 경계를 JS `Date`로 계산해 바인딩하고, 30일 경계 양쪽(29일/31일)을 단위 테스트로 고정 |
| T-01-04-03 | Repudiation | 축소한 `briefing`이 필드를 빠뜨려 아침 브리핑이 조용히 침묵 | medium | mitigate | `briefingLines()`를 실제로 호출하는 통합 단위 테스트로 두 모듈 계약을 고정 |
| T-01-04-04 | Tampering | 제거 대상 테이블·칼럼 참조가 이식 중 섞여 들어감 | high | mitigate | 금지 이름 grep을 완료 기준에 넣고, 실행 시점이 아니라 커밋 전에 잡는다 |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 패키지 설치 0건 — RESEARCH.md `## Package Legitimacy Audit` 판정 |
</threat_model>

<verification>
- `node --test test/store.test.mjs` exit 0 (30건 이상)
- `node --test test/brief.test.mjs` exit 0
- `npm test` exit 0
- `main/store.mjs`에 PostgreSQL 전용 구문·제거 대상 테이블 이름이 없다
</verification>

<success_criteria>
D-05의 살아남는 함수 목록이 `main/store.mjs`에서 전부 구현되고 단위 테스트로 고정되어, 01-05가 `main/ipc.mjs`의 위임을 한 번에 갈아끼울 수 있는 상태가 된다.
</success_criteria>

<output>
Create `.planning/phases/01-embedded-storage/01-04-SUMMARY.md` when done
</output>
