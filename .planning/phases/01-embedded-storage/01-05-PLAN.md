---
phase: 01-embedded-storage
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - main/ipc.mjs
  - main/jobs.mjs
  - main/lifecycle.mjs
  - renderer/today.js
autonomous: true
requirements: [STOR-01]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "앱의 모든 살아있는 기능(캡처·오늘 뷰·프로젝트·대기·인박스·완료 이력·아침 브리핑)이 새 저장소만 거쳐 동작한다 (D-05)"
    - "레거시 IPC 핸들러는 삭제되지 않고 무해한 스텁으로 남아 있다 — 빈 값을 돌려주고 아무것도 쓰지 않는다 (D-06)"
    - "수집·캘린더·백업·주간 리뷰·재개 카드 프리웜·완료 제안 백그라운드 타이머가 하나도 돌지 않는다 (D-06)"
    - "이슈 탭 등 레거시 화면은 빈 채로 열리고, 앱은 뜨고 캡처·오늘 뷰·프로젝트·대기·인박스·브리핑은 완전히 동작한다 (D-06)"
    - "설정 화면이 PostgreSQL 접속 정보 대신 저장소 파일 위치와 상태를 보여준다 (RESEARCH `Runtime State Inventory`의 A3 권고)"
  artifacts:
    - path: main/ipc.mjs
      provides: "store 위임으로 바뀐 핸들러 + 레거시 스텁"
      contains: "ctx.store"
    - path: main/jobs.mjs
      provides: "브리핑·큐 반영·purge만 남은 백그라운드"
      max_lines: 140
  key_links:
    - from: "main/ipc.mjs"
      to: "main/store.mjs"
      via: "itemOps 맵과 나머지 핸들러가 ctx.store의 함수로 위임한다"
      pattern: "ctx\\.store\\."
    - from: "main/jobs.mjs"
      to: "main/store.mjs"
      via: "maybeBrief가 store.briefing을, purgeOnce가 store.purgeDeleted를 부른다"
      pattern: "ctx\\.store\\.(briefing|purgeDeleted)"
  prohibitions:
    - "레거시 스텁이 저장소·파일·네트워크에 쓰기를 해서는 안 된다 — Phase 2가 걷어낼 때까지 무해해야 한다 (D-06)"
---

<!-- planner-discipline-allow: ctx.db -->
<!-- planner-discipline-allow: cfg.db -->
<!-- planner-discipline-allow: collectAll -->
<!-- planner-discipline-allow: prewarmCards -->
<!-- planner-discipline-allow: maybeBackup -->
<!-- planner-discipline-allow: maybeReview -->
<!-- planner-discipline-allow: maybeSuggestDone -->

<objective>
런타임에서 PostgreSQL을 끊어낸다. 살아남는 핸들러는 전부 `ctx.store`로 위임하고, 제거 예정 기능의 IPC는 D-06대로 무해한 스텁으로 내려앉히며, 관련 백그라운드 타이머는 끈다. 이 계획이 끝나면 앱은 Docker 없이 완전히 동작하고, `main/db.mjs`는 아무도 부르지 않는 죽은 파일이 된다(삭제는 01-06).

Purpose: 삭제와 위임 교체를 한 커밋에 섞으면 무엇이 깨졌는지 갈라낼 수 없다. 먼저 아무도 부르지 않는 상태로 만들고, 그 다음에 지운다.

Output: store로만 말하는 `main/ipc.mjs`, 브리핑·큐 반영·purge만 남은 `main/jobs.mjs`, 정리된 트레이 메뉴와 설정 화면.
</objective>

<execution_context>
@D:/AIProject/whenwork/.claude/gsd-core/workflows/execute-plan.md
@D:/AIProject/whenwork/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-embedded-storage/01-CONTEXT.md
@.planning/phases/01-embedded-storage/01-03-SUMMARY.md
@.planning/phases/01-embedded-storage/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: IPC 위임을 store로 갈아끼우고 레거시 채널을 스텁으로 내리기</name>
  <reversibility rating="costly">스텁으로 바뀐 채널은 Phase 2가 UI와 함께 지울 때까지 여러 호출부가 그 반환 형태를 전제한다 — 되돌리려면 스텁과 화면을 같이 고쳐야 한다.</reversibility>
  <files>main/ipc.mjs, main/lifecycle.mjs, renderer/today.js</files>
  <read_first>
    - `main/ipc.mjs` 전체 (01-01이 옮기고 01-02·01-03이 캡처·오늘 뷰를 손댄 상태)
    - `main/preload.cjs` (렌더러가 실제로 부르는 채널 목록 — 이 목록의 채널은 하나도 사라지면 안 된다)
    - `.planning/phases/01-embedded-storage/01-04-SUMMARY.md` (store가 제공하는 함수 이름과 시그니처)
    - `main/index.mjs` 기준 원본의 핸들러 반환 형태: 542-556행(`review:get`), 613-631행(`settings:get`), 1255-1269행(`resumePayload`) — 스텁이 같은 키를 유지해야 화면이 깨지지 않는다
    - `renderer/today.js` 890-940행 (설정 탭이 `settings:get` 응답에서 읽는 필드)
  </read_first>
  <action>
**(1) 살아남는 핸들러의 위임 교체.** `ctx.db.X(...)` 호출을 전부 `ctx.store.X(...)`로 바꾼다. 대상: `itemOps` 맵의 `item:complete`·`item:uncomplete`·`item:assign`·`item:toWaiting`·`item:rename`·`item:remove`·`item:restore`·`item:note`·`project:create`·`project:update`·`project:archive`·`project:move`, 그리고 `item:nudge`·`item:nudgeUndo`·`item:due`·`history:get`·`capture:projects`(약어 힌트). store 함수는 동기이므로 `await`는 없어도 되지만, `itemOps` 등록 패턴을 유지하기 위해 핸들러 자체는 지금 형태를 그대로 둔다.

**(2) 레거시 채널 스텁.** 아래 채널은 **등록을 지우지 않고** 무해한 값을 돌려주게 바꾼다. 저장소·파일·네트워크에 아무것도 쓰지 않고, 외부 프로세스를 띄우지 않는다.
- `issue:promote`, `project:repos`, `item:doneSuggestMute`, `inbox:classify`, `calendar:sync`, `backup:now`, `review:generate` → `{ ok: false }`
- `resume:get`, `resume:sync`, `resume:generate` → 원본 `resumePayload`와 같은 키를 가진 빈 값: `{ ok: false, card: null, fresh: 0, activities: [], issues: [], promoted: [], generating: false, retryAfter: null }`
- `review:get` → 주 계산은 그대로 하고 본문만 비운다: `{ ok: true, label, year, week, generating: false, review: null }`
- `review:openFile` → `{ ok: false }`
- `open:url`·`win:hide`·`app:open`은 저장소와 무관하므로 그대로 둔다.

**(3) `settings:get`.** 응답에서 PostgreSQL 접속 정보를 담던 객체를 빼고, 대신 `store: { file: ctx.store.file, ok: <status().ok>, notice: <status().notice> }`를 넣는다. `settings.json`의 접속 설정을 읽던 코드도 `main/lifecycle.mjs`의 ctx 생성부에서 함께 지운다 — `main/db.mjs`가 사라지면 아무도 읽지 않는 죽은 설정이다. 나머지 키(`values`·`calendar`·`defaults`·`lastBackup`·`file` 등)는 Phase 2가 걷어낼 때까지 형태를 유지한다.

`renderer/today.js`의 설정 탭에서 접속 정보를 그리던 줄(890-940행 사이, `cfg.db`를 읽는 곳)을 저장소 정보로 바꾼다: 파일 경로와 "정상 / 대기 중" 상태를 한 줄로 보여준다. 이 값은 사용자 자신의 데이터 파일 위치라 화면에 그대로 드러내도 된다 — 반대로 오류 안내에는 절대 경로를 넣지 않는다는 규칙(01-02)은 그대로다.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, 또는 요약 줄의 `# fail`이 0이 아니다</fails_when>
    <automated>npm run smoke</automated>
    <fails_when>non-zero exit, 또는 출력에 `SMOKE_OK`가 없다 — 스모크 프로브가 이슈·리뷰·설정 탭을 전부 돌므로 스텁이 화면을 깨뜨리면 여기서 걸린다</fails_when>
  </verify>
  <acceptance_criteria>
    - `! grep -rn "ctx.db" main/` 가 아무것도 찾지 못한다
    - `! grep -rn "cfg.db" renderer/` 가 아무것도 찾지 못한다
    - `main/preload.cjs`가 노출하는 모든 채널 이름이 여전히 `main/ipc.mjs`에 등록되어 있다 (채널 삭제 0건)
    - `main/ipc.mjs`에 `ctx.store.` 호출이 12곳 이상 있다
    - `npm run smoke` 출력에 `SMOKE_OK` — 이슈·리뷰·프로젝트·설정 탭이 스텁 응답으로도 그려진다
    - Docker를 끈 상태에서 앱을 띄워 캡처·완료·프로젝트 생성·대기 전환·마감 지정이 전부 동작한다
  </acceptance_criteria>
  <done>모든 IPC가 새 저장소만 거치고, 레거시 채널은 빈 값을 돌려주며, 앱은 Docker 없이 전 탭이 예외 없이 그려진다.</done>
</task>

<task type="auto">
  <name>Task 2: 레거시 백그라운드 작업과 트레이 메뉴 정리</name>
  <reversibility rating="reversible">작업 함수 제거. Phase 2가 어차피 해당 모듈을 통째로 지우므로 되돌릴 이유도 거의 없다.</reversibility>
  <files>main/jobs.mjs, main/lifecycle.mjs</files>
  <read_first>
    - `main/jobs.mjs` 전체 (01-01이 옮기고 01-03이 큐 반영을 바꾼 상태)
    - `main/lifecycle.mjs`의 `refreshTrayMenu` (01-03이 저장소 상태 줄까지 손댄 상태)
    - `main/brief.mjs` (아침 브리핑 판단 로직 — 여기는 손대지 않는다)
    - `test/brief.test.mjs`, `test/calendar.test.mjs`, `test/collect.test.mjs`, `test/repo.test.mjs`, `test/suggest.test.mjs`, `test/vault.test.mjs` (순수 모듈 테스트라 작업 제거 후에도 계속 통과해야 한다)
  </read_first>
  <action>
**(1) `main/jobs.mjs`에서 제거할 것.** 함수와 그 타이머 등록, 그리고 딸린 상태 플래그·상수를 함께 지운다: git·이슈 수집(`collectAll`), 캘린더 동기화와 오늘 일정 조회, SQL 덤프 백업(`backupNow`·`maybeBackup`), 주간 리뷰 생성(`makeWeeklyReview`·`maybeReview`·`reviewNotice`·`weekOf`), 재개 카드(`resumePayload`·`buildResumeCard`·`prewarmCards`·`findProject`), 완료 제안(`maybeSuggestDone`), AI 호출 계량(`withAiLog`). 이들만 쓰던 import도 함께 지운다.

**(2) `main/jobs.mjs`에 남을 것.** `replayQueueOnce(ctx)`(시작 시 1회), `maybeBrief(ctx)`(지연 1회 + 분 단위 점검), `purgeOnce(ctx)`(시작 시 1회). `maybeBrief`는 저장소 온라인 판정을 `ctx.store.status().ok`로 바꾸고 브리핑 재료를 `ctx.store.briefing(...)`에서 가져온다. 일정·리뷰 인자는 빈 값으로 넘긴다 — `main/brief.mjs`의 `briefingLines()`가 falsy 가드로 이미 감싸고 있어 문구가 깨지지 않는다. `purgeOnce`는 `ctx.store.purgeDeleted(PURGE_DAYS)`를 부르고 실패를 조용히 삼킨다.

`scheduleJobs(ctx)`에 남는 등록은 이 셋뿐이다. 큐 반영 1회는 `!SMOKE` 가드 밖, 나머지는 지금처럼 `!SMOKE` 안쪽에 둔다.

**(3) 트레이 메뉴(`main/lifecycle.mjs`).** 삭제된 작업을 부르던 항목과 그 상태 줄을 지운다: 주간 리뷰 초안 만들기, 지금 수집, 지금 백업, 마지막 수집 시각 줄, 백업 실패/마지막 백업 줄, 주간 리뷰 실패 줄. 남기는 것은 오늘 뷰·퀵캡처·저장소 상태 줄(01-03이 만든 것)·로그인 시 자동 시작·창 위치 초기화·종료다. 구분선이 연달아 남지 않게 정리한다.

**(4) 확인.** 남은 순수 모듈 테스트는 모듈 자체를 지우지 않았으므로 그대로 통과해야 한다. 통과 수가 줄면 무언가를 같이 지운 것이다.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, 또는 요약 줄의 `# fail`이 0이 아니거나 `# pass` 수가 01-05 시작 전보다 줄었다</fails_when>
    <automated>npm run smoke</automated>
    <fails_when>non-zero exit, 또는 출력에 `SMOKE_OK`가 없다</fails_when>
    <automated>node -e "const s=require('node:fs').readFileSync('main/jobs.mjs','utf8'); const n=(s.match(/setInterval|setTimeout/g)||[]).length; if(n>4){console.error('too many timers left in jobs.mjs: '+n);process.exit(1)}"</automated>
    <fails_when>non-zero exit(= stderr에 `too many timers left in jobs.mjs: N`이 찍힌다)</fails_when>
    <human-check>앱을 몇 분 띄워 두고 트레이 메뉴를 연다. 수집·백업·주간 리뷰 항목이 없고, git·gh·glab·claude 프로세스가 하나도 뜨지 않으며, 아침 브리핑 시각을 방금 지난 시각으로 바꿨을 때 알림이 한 번 뜨는지 확인한다.</human-check>
  </verify>
  <acceptance_criteria>
    - `main/jobs.mjs`가 140줄 미만이고 `replayQueueOnce`·`maybeBrief`·`purgeOnce` 세 함수만 export하거나 `scheduleJobs`에서 부른다
    - `! grep -rn "collectAll\|prewarmCards\|maybeBackup\|maybeReview\|maybeSuggestDone" main/jobs.mjs main/lifecycle.mjs` 가 아무것도 찾지 못한다
    - `main/jobs.mjs`가 `ctx.store.briefing`과 `ctx.store.purgeDeleted`를 부른다
    - 트레이 메뉴 템플릿에 수집·백업·주간 리뷰 항목이 없다
    - `npm test` exit 0이고 통과 수가 줄지 않았다
    - `npm run smoke` 출력에 `SMOKE_OK`
  </acceptance_criteria>
  <done>백그라운드에는 큐 반영 1회·아침 브리핑·purge만 남고, 트레이 메뉴에 죽은 작업 항목이 없으며, 외부 CLI를 부르는 타이머가 하나도 돌지 않는다.</done>
</task>

</tasks>

## Artifacts this phase produces

| Kind | Symbol / path | Owner |
|---|---|---|
| module | `main/lifecycle.mjs` — `bootstrap()` | 01-01 |
| module | `main/ipc.mjs` — `registerIpc(ctx)`, `saveCapture(ctx, title, context)` | 01-01 / 01-03 |
| module | `main/jobs.mjs` — `scheduleJobs(ctx)`, `replayQueueOnce(ctx)`, `maybeBrief(ctx)`, `purgeOnce(ctx)` | 01-01 / 01-03 / 01-05 |
| module | `main/store.mjs` — `createStore(file)`, `MIGRATIONS`, `schemaTables()`, `NewerSchemaError` | 01-02 |
| function (store) | `open`, `close`, `reopen`, `checkpoint`, `status`, `insertCaptures`, `getViewState`, `logEvent` | 01-02 |
| function (store) | `getProjects`, `createProject`, `updateProject`, `archiveProject`, `moveProject`, `completeItem`, `uncompleteItem`, `assignProject`, `setDue`, `toWaiting`, `renameItem`, `removeItem`, `restoreItem`, `setNote`, `nudgeItem`, `nudgeRestore`, `getInbox`, `purgeDeleted`, `getHistory`, `briefing` | 01-04 |
| function (queue) | `replayPending(consume)` — 기존 `drain()` 대체 | 01-03 |
| IPC 응답 필드 | `settings:get`의 `store: { file, ok, notice }` (PostgreSQL 접속 정보 대체) | 01-05 |
| ctx field | `ctx.store`, `ctx.storeOk`, `ctx.pending` | 01-02 / 01-03 |
| test | `test/store.test.mjs` | 01-02 / 01-04 |
| test | `test/queue.test.mjs` (재작성) | 01-03 |
| test | `test/capture-crash-smoke.test.mjs` | 01-07 |
| CLI flag | `--inject-capture=<title>`, `--smoke-data=<dir>` (둘 다 `--smoke`와 함께만) | 01-07 |
| npm script | `smoke:crash` | 01-07 |
| stdout marker | `CAPTURE_INJECTED <id>` | 01-07 |
| file name | `store.sqlite`, `store.corrupt-<타임스탬프>.sqlite`, `backups/store-v<이전버전>-<YYYYMMDD>.sqlite` | 01-02 |
| file name | `queue.pending-<epoch>.jsonl` | 01-03 |
| deleted | `main/db.mjs`, `main/backup.mjs`, `test/backup.test.mjs` | 01-06 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (레거시 채널) | UI는 여전히 이 채널들을 부른다. 스텁이 무해해야 하는 이유 |
| main → 외부 프로세스(git·gh·glab·claude) | 이 계획이 **닫는** 경계다 — 타이머가 사라지면 호출 자체가 없어진다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05-01 | Tampering | 스텁이 실수로 쓰기를 수행 | high | mitigate | 스텁 본문은 값을 돌려주는 한 줄뿐이며 저장소·파일·프로세스 호출을 포함하지 않는다 (D-06) |
| T-01-05-02 | Denial of Service | 채널을 통째로 지워 렌더러 호출이 미등록 채널 오류로 터짐 | high | mitigate | `main/preload.cjs` 채널 목록 대조를 완료 기준에 넣고, 스모크가 전 탭을 돌며 확인한다 |
| T-01-05-03 | Information Disclosure | 설정 화면에 남은 DB 계정·호스트 표시 | medium | mitigate | 접속 정보 블록을 저장소 파일 정보로 교체 (RMV-07의 개인 흔적 제거와 같은 방향) |
| T-01-05-04 | Elevation of Privilege | 백그라운드가 계속 외부 CLI를 실행 | medium | mitigate | 관련 타이머와 함수를 제거하고, 남은 타이머 수를 자동 검증으로 상한 |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 패키지 설치 0건 — RESEARCH.md `## Package Legitimacy Audit` 판정 |
</threat_model>

<verification>
- `npm test` exit 0, 통과 수 유지
- `npm run smoke` 출력에 `SMOKE_OK` (전 탭 통과)
- PostgreSQL 위임 호출이 코드베이스에 없다
- Docker를 끈 상태에서 캡처·완료·프로젝트·대기·마감이 모두 동작한다
</verification>

<success_criteria>
`main/db.mjs`를 아무도 부르지 않는 상태가 되고, 앱은 Docker 없이 전 기능이 동작하며, 레거시 화면은 빈 채로 예외 없이 열린다.
</success_criteria>

<output>
Create `.planning/phases/01-embedded-storage/01-05-SUMMARY.md` when done
</output>
