---
phase: 01-embedded-storage
plan: 06
type: execute
wave: 5
depends_on: ["01-05"]
files_modified:
  - package.json
  - main/db.mjs
  - main/backup.mjs
  - test/backup.test.mjs
autonomous: false
requirements: [STOR-01]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`main/db.mjs`와 pg 풀이 코드베이스에 없다 — 두 저장소가 공존하는 기간은 없다 (D-05)"
    - "`main/backup.mjs`와 `test/backup.test.mjs`가 삭제되었다. 백업은 Phase 3의 JSON 내보내기가 대신한다 (D-09, RMV-04 선행)"
    - "`pg`가 `devDependencies`에 있고 앱 런타임 import는 0건이다 — electron-builder가 설치본에 싣지 않는다 (D-07)"
    - "삭제 직전 상태가 `v-personal` 태그로 보존되어, 개인용 PostgreSQL 코드로 되돌아갈 경로가 git에 남는다 (D-05)"
    - "앱은 PostgreSQL·Docker 없이 실행되고 프로젝트·항목·이벤트가 앱 데이터 폴더 안 단일 파일에 저장된다 (STOR-01)"
  artifacts:
    - path: package.json
      provides: "pg를 devDependencies로 옮긴 의존성 선언"
      contains: "devDependencies"
  key_links:
    - from: "package.json"
      to: "electron-builder 패키징"
      via: "production dependencies만 설치본에 복사되므로 pg가 devDependencies면 자동으로 빠진다"
      pattern: "devDependencies"
  prohibitions:
    - "`v-personal` 태그를 만들지 않은 채 삭제를 진행해서는 안 된다 — 개인용 PostgreSQL 경로로 돌아갈 유일한 표식이다 (D-05)"
---

<!-- planner-discipline-allow: db\.mjs\|backup\.mjs -->
<!-- planner-discipline-allow: db.mjs -->
<!-- planner-discipline-allow: backup.mjs -->

<objective>
아무도 부르지 않게 된 PostgreSQL 코드를 실제로 지운다: `main/db.mjs`, `main/backup.mjs`, `test/backup.test.mjs`를 삭제하고 `pg`를 `devDependencies`로 내린다. 그 전에 되돌아갈 표식(`v-personal` 태그)을 어디에 달지 확정한다.

Purpose: 삭제는 이 페이즈에서 유일하게 되돌릴 수 없는 문이다(D-05 one-way). 문을 지나기 전에 표식을 남기고, 지난 뒤에는 설치본에 pg가 실리지 않는다는 사실까지 확인한다.

Output: 삭제된 세 파일, `devDependencies`로 내려간 `pg`, `v-personal` 태그.
</objective>

<execution_context>
@D:/AIProject/whenwork/.claude/gsd-core/workflows/execute-plan.md
@D:/AIProject/whenwork/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-embedded-storage/01-CONTEXT.md
@.planning/phases/01-embedded-storage/01-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: v-personal 태그를 어느 지점에 달 것인가</name>
  <decision>개인용 PostgreSQL 버전을 보존할 `v-personal` 태그를 어느 커밋에 달지 정한다.</decision>
  <context>
다음 작업이 `main/db.mjs`를 삭제한다. CONTEXT.md D-05가 이 결정을 **one-way**로 표시했고("삭제 후에는 PG 경로로 되돌릴 수 없고, 개인용 코드는 태그로만 보존된다"), 삭제 자체는 이미 사용자가 확정한 결정이라 여기서 다시 묻지 않는다. 정해지지 않은 것은 **표식의 위치** 하나다.

무게: Phase 3(DATA-04)의 이전 스크립트가 아직 없는 시점이라, 작성자의 실제 캡처 이력은 여전히 로컬 PostgreSQL 안에만 있다. 그 데이터를 읽는 코드로 돌아가야 할 일이 생기면 이 태그가 유일한 입구다. `docker-compose.yml`과 `db` npm 스크립트는 Phase 3가 끝날 때까지 지우지 않으므로 컨테이너 자체는 그대로 살아 있다.
  </context>
  <options>
    <option id="pre-phase">
      <name>이 페이즈 시작 직전 커밋</name>
      <pros>PostgreSQL 앱이 마지막으로 온전히 돌던 지점이다. 태그를 체크아웃하면 그때의 앱이 그대로 뜬다 — 되돌아가는 목적에 정확히 맞는다</pros>
      <cons>분할(01-01)과 새 저장소가 포함되지 않아, 이 페이즈의 작업을 다시 가져오려면 cherry-pick이 필요하다</cons>
    </option>
    <option id="pre-delete">
      <name>db.mjs 삭제 직전 커밋 (다음 작업의 HEAD)</name>
      <pros>분할·새 저장소·PG 코드가 **전부** 들어 있는 유일한 지점이다. 두 저장소의 코드를 나란히 놓고 비교하기 좋고, Phase 3 이전 스크립트를 쓸 때 참고하기 편하다</pros>
      <cons>그 시점의 앱은 이미 store로만 동작하므로 "PG로 돌아간다"는 뜻은 아니다 — PG 코드가 파일로 남아 있을 뿐이다</cons>
    </option>
    <option id="both">
      <name>둘 다 (`v-personal`과 `v-personal-lastmix`)</name>
      <pros>돌아갈 지점과 비교할 지점을 분리해 둘 다 잃지 않는다</pros>
      <cons>태그가 둘로 늘어 Phase 5의 공개 리포 분리 때 무엇을 남길지 한 번 더 판단해야 한다</cons>
    </option>
  </options>
  <action>세 선택지를 그대로 제시하고 사용자의 답을 기다린다. 답이 오면 `git tag -a <이름> <sha> -m "개인용 PostgreSQL 버전 보존 (D-05)"`로 태그를 만들고, 만든 태그 이름과 커밋 해시를 SUMMARY에 적는다. 답 없이 다음 작업으로 넘어가지 않는다.</action>
  <resume-signal>선택지 id(`pre-phase` / `pre-delete` / `both`)를 답하거나, 다른 커밋을 지정한다.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: PostgreSQL 코드 삭제와 pg 격하</name>
  <reversibility rating="one-way">`main/db.mjs`와 pg 런타임 경로를 지우면 이 앱에서 PostgreSQL로 돌아가는 길은 Task 1이 만든 태그밖에 없다. 사용자가 CONTEXT.md D-05에서 이 선택을 이미 확정했으므로 문은 이미 지나간 상태이고, 앞선 체크포인트는 삭제 여부가 아니라 표식의 위치만 물었다.</reversibility>
  <precondition>Task 1의 태그가 실제로 만들어져 있다 — `git tag --list` 출력에 그 이름이 있어야 한다.</precondition>
  <files>package.json, main/db.mjs, main/backup.mjs, test/backup.test.mjs</files>
  <read_first>
    - `package.json` (`dependencies`·`devDependencies`·`build.files` 블록)
    - `test/backup.test.mjs` (삭제 대상 — 92-95행의 스키마 대조 가드가 01-02의 `test/store.test.mjs`로 이미 이관되었는지 먼저 확인한다. 안 되어 있으면 삭제하지 말고 먼저 옮긴다)
    - `.planning/phases/01-embedded-storage/01-02-SUMMARY.md` (STOR-05 가드 테스트가 어디에 있는지)
    - `.planning/phases/01-embedded-storage/01-05-SUMMARY.md` (위임 교체가 끝났다는 확인)
  </read_first>
  <action>
**(1) 삭제.** `main/db.mjs`, `main/backup.mjs`, `test/backup.test.mjs` 세 파일을 `git rm`으로 지운다. 삭제 전에 `test/backup.test.mjs`의 "스키마의 모든 테이블이 백업에 담긴다" 대조 가드가 `test/store.test.mjs`의 STOR-05 가드로 이미 살아 넘어갔는지 확인한다 — 넘어가지 않았으면 먼저 옮기고 지운다(D-09).

**(2) `pg` 격하.** `package.json`에서 `pg` 항목을 `dependencies`에서 `devDependencies`로 옮긴다. 버전(`^8.13.0`)은 바꾸지 않고 새로 설치하지 않는다. Phase 3의 `tools/` 이전 스크립트만 이것을 쓴다(D-07). `dependencies`가 비면 빈 객체로 남기지 말고 키를 지운다. `build.files`는 `main/**/*`라 새 모듈이 자동 포함되므로 손대지 않는다.

**(3) 죽은 참조 정리.** 세 파일을 import하던 곳이 남아 있으면(01-05가 다 끊었어야 한다) 함께 지운다. `docker-compose.yml`과 `db` npm 스크립트는 **지우지 않는다** — Phase 3 이전 스크립트가 그 컨테이너를 읽어야 한다.

**(4) 확인.** `npm test`가 통과하되 통과 수는 `test/backup.test.mjs`가 담당하던 만큼 줄어든다. 줄어든 폭이 그 파일의 테스트 수와 맞는지 확인해 SUMMARY에 적는다 — 예상보다 더 줄었다면 무언가를 같이 끊은 것이다.

`package.json`은 반드시 Node로 읽고 쓰거나 에디터로 고친다. PowerShell로 쓰면 BOM이 붙어 앱이 빈 설정으로 시작한다(CLAUDE.md).
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, 또는 요약 줄의 `# fail`이 0이 아니다</fails_when>
    <automated>npm run smoke</automated>
    <fails_when>non-zero exit, 또는 출력에 `SMOKE_OK`가 없다</fails_when>
    <automated>node -e "const p=require('./package.json'); if(p.dependencies&&p.dependencies.pg){console.error('pg is still a runtime dependency');process.exit(1)} if(!p.devDependencies||!p.devDependencies.pg){console.error('pg missing from devDependencies');process.exit(1)}"</automated>
    <fails_when>non-zero exit(= stderr에 `pg is still a runtime dependency` 또는 `pg missing from devDependencies`가 찍힌다)</fails_when>
    <automated>test ! -f main/db.mjs && test ! -f main/backup.mjs && test ! -f test/backup.test.mjs && echo DELETED_OK</automated>
    <fails_when>`DELETED_OK`가 출력되지 않는다(= 셋 중 하나라도 파일이 남아 있다)</fails_when>
    <automated>test -n "$(git tag --list 'v-personal*')" && echo TAG_OK</automated>
    <fails_when>`TAG_OK`가 출력되지 않는다(= Task 1의 태그가 만들어지지 않았다)</fails_when>
  </verify>
  <acceptance_criteria>
    - `main/db.mjs`·`main/backup.mjs`·`test/backup.test.mjs` 세 파일이 워킹트리와 git 인덱스 모두에 없다
    - `! grep -rln "db\.mjs\|backup\.mjs" main/ test/ renderer/` 가 아무것도 찾지 못한다
    - `package.json`의 `devDependencies`에 `pg`가 있고 `dependencies`에는 없다
    - `main/` 아래에 pg 패키지를 런타임에 불러오는 코드가 없다 — `! grep -rn "'pg'" main/ renderer/`
    - `git tag --list 'v-personal*'` 출력이 비어 있지 않다
    - `npm test` exit 0, `npm run smoke` 출력에 `SMOKE_OK`
    - Docker 컨테이너를 **정지시킨 상태**에서 앱을 띄워 캡처·오늘 뷰·프로젝트·대기·인박스·완료 이력이 전부 동작한다 (STOR-01)
  </acceptance_criteria>
  <done>PostgreSQL 코드가 리포에서 사라지고, `pg`는 개발 의존성으로만 남으며, 앱은 Docker를 끈 채 완전히 동작한다.</done>
</task>

</tasks>

## Artifacts this phase produces

| Kind | Symbol / path | Owner |
|---|---|---|
| module | `main/lifecycle.mjs` — `bootstrap()` | 01-01 |
| module | `main/ipc.mjs` — `registerIpc(ctx)`, `saveCapture(ctx, title, context)` | 01-01 / 01-03 |
| module | `main/jobs.mjs` — `scheduleJobs(ctx)`, `replayQueueOnce(ctx)`, `maybeBrief(ctx)`, `purgeOnce(ctx)` | 01-01 / 01-03 / 01-05 |
| module | `main/store.mjs` — `createStore(file)`, `MIGRATIONS`, `schemaTables()`, `NewerSchemaError` | 01-02 |
| function (store) | `open`, `close`, `reopen`, `checkpoint`, `status`, `insertCaptures`, `getViewState`, `logEvent` | 01-02 |
| function (store) | `getProjects`, `createProject`, `updateProject`, `archiveProject`, `moveProject`, `completeItem`, `uncompleteItem`, `assignProject`, `setDue`, `toWaiting`, `renameItem`, `removeItem`, `restoreItem`, `setNote`, `nudgeItem`, `nudgeRestore`, `getInbox`, `purgeDeleted`, `getHistory`, `briefing` | 01-04 |
| function (queue) | `replayPending(consume)` — 기존 `drain()` 대체 | 01-03 |
| IPC 응답 필드 | `settings:get`의 `store: { file, ok, notice }` | 01-05 |
| ctx field | `ctx.store`, `ctx.storeOk`, `ctx.pending` | 01-02 / 01-03 |
| git tag | `v-personal` (개인용 PostgreSQL 버전 보존) | 01-06 |
| test | `test/store.test.mjs` | 01-02 / 01-04 |
| test | `test/queue.test.mjs` (재작성) | 01-03 |
| test | `test/capture-crash-smoke.test.mjs` | 01-07 |
| CLI flag | `--inject-capture=<title>`, `--smoke-data=<dir>` (둘 다 `--smoke`와 함께만) | 01-07 |
| npm script | `smoke:crash` | 01-07 |
| stdout marker | `CAPTURE_INJECTED <id>` | 01-07 |
| file name | `store.sqlite`, `store.corrupt-<타임스탬프>.sqlite`, `backups/store-v<이전버전>-<YYYYMMDD>.sqlite` | 01-02 |
| file name | `queue.pending-<epoch>.jsonl` | 01-03 |
| deleted | `main/db.mjs`, `main/backup.mjs`, `test/backup.test.mjs` | 01-06 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 리포 ↔ 설치본 | `dependencies`에 있는 것만 설치본에 실린다 — pg가 사용자 기기에 들어가는지가 여기서 갈린다 |
| git 히스토리 | 삭제된 파일은 히스토리에 남는다. 공개 리포 분리는 Phase 5(REL-04)의 몫 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06-01 | Denial of Service | 되돌릴 표식 없이 PG 경로 삭제 | high | mitigate | Task 1의 `checkpoint:decision`으로 `v-personal` 태그 위치를 확정하고, Task 2가 태그 존재를 precondition으로 검사한다 |
| T-01-06-02 | Tampering | STOR-05 가드 테스트가 `test/backup.test.mjs`와 함께 사라짐 | high | mitigate | 삭제 전 가드가 `test/store.test.mjs`로 이관됐는지 확인하고, 통과 수 감소 폭이 예상과 맞는지 대조한다 (D-09) |
| T-01-06-03 | Information Disclosure | pg가 설치본에 실려 사용자 기기에 불필요한 DB 클라이언트가 배포됨 | medium | mitigate | `devDependencies` 격하 + `package.json` 자동 검증. electron-builder는 production dependencies만 복사한다 (D-07) |
| T-01-06-04 | Information Disclosure | `package.json`을 PowerShell로 써서 BOM이 붙어 앱이 빈 설정으로 시작 | medium | mitigate | Node 또는 에디터로만 수정한다 (CLAUDE.md의 알려진 함정) |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | 신규 설치 0건 — `pg`는 이미 설치된 패키지의 위치 이동뿐이다. RESEARCH.md `## Package Legitimacy Audit` 판정 |
</threat_model>

<verification>
- `v-personal` 태그 존재
- 세 파일 삭제 확인
- `package.json`에서 pg가 devDependencies에만 있음
- `npm test` exit 0, `npm run smoke` 출력에 `SMOKE_OK`
- Docker 정지 상태에서 앱 전 기능 동작
</verification>

<success_criteria>
PostgreSQL 코드가 리포에서 사라지고, 개인용 버전은 태그로 보존되며, Docker를 끈 채 앱이 완전히 동작한다.
</success_criteria>

<output>
Create `.planning/phases/01-embedded-storage/01-06-SUMMARY.md` when done
</output>
